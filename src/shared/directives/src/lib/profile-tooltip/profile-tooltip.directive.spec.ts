import { ProfileTooltipDirective } from './profile-tooltip.directive';

describe('ProfileTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
